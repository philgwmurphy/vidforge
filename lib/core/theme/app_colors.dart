import 'package:flutter/material.dart';

/// VidForge color palette
class AppColors {
  AppColors._();

  // Primary colors
  static const primary = Color(0xFF6366F1);
  static const primaryLight = Color(0xFF818CF8);
  static const primaryDark = Color(0xFF4F46E5);

  // Accent colors
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFF34D399);
  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFBBF24);
  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFF87171);
  static const info = Color(0xFF3B82F6);
  static const infoLight = Color(0xFF60A5FA);

  // Dark theme backgrounds
  static const darkBgPrimary = Color(0xFF0F0F0F);
  static const darkBgSecondary = Color(0xFF1A1A1A);
  static const darkBgTertiary = Color(0xFF262626);
  static const darkBgElevated = Color(0xFF2D2D2D);

  // Dark theme surfaces
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkSurfaceVariant = Color(0xFF2A2A2A);
  static const darkBorder = Color(0xFF3A3A3A);

  // Dark theme text
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFA3A3A3);
  static const darkTextTertiary = Color(0xFF737373);
  static const darkTextDisabled = Color(0xFF525252);

  // Light theme backgrounds
  static const lightBgPrimary = Color(0xFFFFFFFF);
  static const lightBgSecondary = Color(0xFFF5F5F5);
  static const lightBgTertiary = Color(0xFFE5E5E5);
  static const lightBgElevated = Color(0xFFFFFFFF);

  // Light theme surfaces
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFF5F5F5);
  static const lightBorder = Color(0xFFE0E0E0);

  // Light theme text
  static const lightTextPrimary = Color(0xFF171717);
  static const lightTextSecondary = Color(0xFF525252);
  static const lightTextTertiary = Color(0xFF737373);
  static const lightTextDisabled = Color(0xFFA3A3A3);

  // Gradient colors
  static const gradientStart = Color(0xFF6366F1);
  static const gradientEnd = Color(0xFF8B5CF6);

  // Video timeline colors
  static const timelineTrack = Color(0xFF3A3A3A);
  static const timelineProgress = Color(0xFF6366F1);
  static const timelineSelection = Color(0xFF6366F1);
  static const timelineHandle = Color(0xFFFFFFFF);
}
